# PostgreSQL 설정 (Railway 클라우드)
# Railway에서 받은 연결 정보로 업데이트하세요
POSTGRES_HOST=containers-us-west-XX.railway.app
POSTGRES_DB=railway
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_railway_password_here
POSTGRES_PORT=5432

# OpenAI API 설정
# https://platform.openai.com/api-keys 에서 발급받으세요
OPENAI_API_KEY=your-openai-api-key-here

# Flask 설정
FLASK_ENV=development
FLASK_DEBUG=True
FLASK_APP=app_postgresql_api.py

# 서버 설정
HOST=0.0.0.0
PORT=5000

# 배포 설정
# Railway에서 자동으로 설정됩니다
